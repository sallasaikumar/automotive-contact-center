version: 1
backend:
  phases:
    build:
      commands:
        - npm ci
frontend:
  phases:
    build:
      commands:
        - npm run build || echo "No build script"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
